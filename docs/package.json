{
  "name": "@hive/docs",
  "version": "0.1.0",
  "description": "Documentation site for Hive",
  "private": true,
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "4.90.1"
  },
  "scripts": {
    "predev": "node scripts/generate-blog-feed.mjs",
    "dev": "vitepress dev",
    "prebuild": "node scripts/generate-blog-feed.mjs",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "aube": {
    "allowBuilds": {
      "esbuild": true,
      "sharp": true,
      "workerd": true
    }
  }
}
