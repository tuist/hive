if command -v fnox &> /dev/null; then
  SECRET_KEY_BASE=$(fnox get SECRET_KEY_BASE)
  DATABASE_URL=$(fnox get DATABASE_URL)
  POSTGRES_PASSWORD=$(fnox get POSTGRES_PASSWORD)
  GOOGLE_CLIENT_ID=$(fnox get GOOGLE_CLIENT_ID)
  GOOGLE_CLIENT_SECRET=$(fnox get GOOGLE_CLIENT_SECRET)
fi
