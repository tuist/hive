#!/bin/sh

# Sets and samples the release environment.
# It will be sourced before the release starts.

if [ -z "$SENTRY_RELEASE" ]; then
  export SENTRY_RELEASE="${HIVE_GIT_COMMIT:-${RELEASE_VSN}}"
fi
